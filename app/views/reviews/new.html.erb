<h1>  Review </h1>

<div class="mt-3">
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :content %>
    <%= f.input :rating %>
    <%= f.submit "Submit", class: "btn btn-primary mt-3" %>
  <% end %>
</div>
